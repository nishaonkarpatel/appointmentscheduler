# Appointment Scheduler – Streamlit + Dialogflow Messenger

- Streamlit app that embeds Dialogflow Messenger as a floating popup.
- Run locally: `pip install -r requirements.txt` then `streamlit run app.py`
